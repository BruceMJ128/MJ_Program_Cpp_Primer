#include <string>
#include <map>
#include <vector>
#include <fstream>
#include <iostream>

using namespace std;

int main()
{
	map<string, vector<int>> m1;
	string s1;
	vector<int> v1;
	
	pair<map<string, vector<int>>::iterator, bool> ret.insert(make_pair(s1,v1));
	
	return 0;
}
