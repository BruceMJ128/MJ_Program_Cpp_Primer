#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
	vector<int> v1(10, 42);
	
	vector<int> v2(10);
	for(vector(int)::size_type i=0; i!=10; ++i)
	{
		v2.push_back(42);
	}
	
}
